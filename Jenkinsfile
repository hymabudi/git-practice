pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git branch: 'main', url: 'https://github.com/hymabudi/git-practice.git'
            }
        }
        stage('budi') {
            steps {
                echo 'budi'
            }
        }
    }
}
